{
  "name": "mindcoding-institute",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && npx --yes wrangler pages deploy dist --project-name=mindcoding-institute --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "@webtui/css": "^0.1.3",
    "astro": "^5.6.0"
  }
}
